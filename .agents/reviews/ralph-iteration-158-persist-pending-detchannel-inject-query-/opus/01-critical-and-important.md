# Critical And Important Issues

No Critical or Important issues found in this pass.

The EVTC v2 layout is deterministic, v1 sections remain accepted only at the legacy 39-byte length, v2 pending tables are length-checked and rejected on detached sections, and restored pending answers consume both the serialized mirror and the replay/recording fault-plan path exactly once.
