# Action Items

No blocking action items.

Optional follow-up:

- Add or expose an explicit page-channel attachment signal for test fixtures, so `spawn_store_at` can assert "fast path connected" directly instead of relying on the corrupt-cross-check sibling helper to prove the same construction path.
