- Add a `NotFile`/symlink regression so the flat-cache contract is pinned.
  Status: done at crates/dh-worker/src/image_resolver.rs:565.

- Keep `blake3.workspace = true` alphabetized with dependencies.
  Status: done in crates/dh-worker/Cargo.toml.

- Run the focused resolver, blkfile, worker, bench, fmt, and whitespace checks.
  Status: done, all passed.

