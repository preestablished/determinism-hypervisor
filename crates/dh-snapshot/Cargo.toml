[package]
name = "dh-snapshot"
version.workspace = true
edition.workspace = true
license.workspace = true

[dev-dependencies]
# M4 store-integration readiness gate (bead 4nj): pins the sibling
# snapstore-client surface at compile time via tests/snapstore_readiness.rs.
snapstore-client.workspace = true
