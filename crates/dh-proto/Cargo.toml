[package]
name = "dh-proto"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
# Cross-repo facade for the shared non-hypervisor contract types. The
# hypervisor::v1 placeholder structs in determinism-proto are superseded by
# the local codegen below (docs/decisions/proto-seam.md).
determinism-proto = { workspace = true, features = ["common"] }
prost.workspace = true
tonic.workspace = true

[build-dependencies]
protoc-bin-vendored.workspace = true
tonic-build.workspace = true
